### Binários

Esse diretório deve conter apenas os binários criados pela compilação do projetos, utilizando o comando:
```
mkdir $local_de_instalacao/bin/
javac -d $local_de_instalacao/bin/ $local_de_instalacao/App.java
```